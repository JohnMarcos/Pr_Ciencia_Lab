<h1>Mis Cursos</h1>
<h1><%= current_user.first_name %> <%= current_user.last_name %></h1>
<% @enrollments.each do |course_enrollment| %>
  <% if c.lessons.nil? %>
    <p>No hay lecciones</p>
  <% else %>
<%= course_enrollment.name %>
<h2>Curso: <%= link_to e.name, course_enrollment_lesson_path(:course_id => .id, :enrollment_id => c.enrollments[0].id, :id => c.lessons[0].id) %></h2>
<p>Descripción del curso: <%= c.description %></p>
<p>Profesor: <%= c.user.first_name %> <%= c.user.last_name %></p>
<p>Lecciones: <%= c.lessons.count %></p>
<p>Comentarios: <%= c.comments.count %></p>
<% end %>
<br/>
<%= link_to "Home", root_path %>
